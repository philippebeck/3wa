{# ********************** #}
{# ***** ATLAS VIEW ***** #}
{# ********************** #}


{% extends 'layout/layout.twig' %}

{% block title %}Atlas de cartes célestes{% endblock %}

{% block description %}La section Atlas est dédiée à l'histoire de l'astronomie au travers d'atlas célestes rares & anciens. Vous y trouverez donc des cartes du ciel en couleurs couvrant une période de près d'un millier d'années, ainsi que des liens Wikipedia pour certains atlas & auteurs{% endblock %}


{% block main %}

<main id="atlas">

  <p class="intro cancel-sea double-slight-lg inset-spread-sm anime-slideB-sm center">
    Cliquer sur un <b>bouton vert
    <i class="fa fa-map-o fa-lg" aria-hidden="true"></i>
    </b> donne accès aux <strong>cartes de l'atlas</strong><br>
    Cliquer sur un <b>bouton bleu</b> ouvre l'<strong>article Wikipedia de l'atlas ou de l'auteur</strong><br>
    (l'article peut être en anglais, si la version franophone est inexistante)
  </p>

  <!-- Atlas table -->
  <table class="cancel-sea outset-lg inset-blur anime-slideL-sm">

    <!-- Table header -->
    <thead>
      <tr>
        <th>Atlas</th>
        <th>Cartes</th>
        <th>Auteur</th>
      </tr>
    </thead>

    <tbody>

      <!-- Loop on all atlas -->
      {% for atlas in allAtlas %}
      <tr itemscope itemtype="http://schema.org/CreativeWork">

        <!-- Atlas name -->
        <td>
          {% if atlas.wiki %}
          <a class="btn primary-light solid-rounder help anime-flipY-md" href="{{ atlas.wiki }}" title="Wiki sur l'atlas" itemprop="about">
            <span itemprop="name">{{ atlas.name }}</span>
          </a>
          {% else %}
          <span itemprop="name">{{ atlas.name }}</span>
          {% endif %}
        </td>

        <!-- Read atlas button -->
        <td>
          <a class="btn valid-btn solid-round blur crosshair anime-flipX-md" href="{{ url('atlas!read', {'id': atlas.id}) }}" title="Voir l'atlas" itemprop="url">
            <i class="fa fa-map-o fa-lg" aria-hidden="true"></i>
            <div itemprop="copyrightYear">{{ atlas.year }}</div>
          </a>
        </td>

        <!-- Atlas author -->
        <td>
          {% if atlas.author_wiki %}
          <a class="btn secondary-light solid-rounder help anime-flipY-md" href="{{ atlas.author_wiki }}" title="Wiki sur l'auteur">
            <span itemprop="author">{{ atlas.author_name }}</span>
          </a>
          {% else %}
          <span itemprop="author">{{ atlas.author_name }}</span>
          {% endif %}
        </td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
</main>

{% endblock %}
